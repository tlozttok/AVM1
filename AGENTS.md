AVM1是一个旨在构建以LLM对话为核心的通用计算模型架构的项目

它：
1.不是Agent架构
2.不提供具体的提示词（工具调用、环境提示除外）
3.不和现实操作系统、硬件、网络进行交互
4.自成体系，有内存模型和计算调度模型
5.疯狂地不做你认为必须做的事

如何参与设计它：
1.你需要不参与python和LLM的工程生产，你需要精通计算机科学史
2.LLM从出现至今时间很短，你不需要知道任何关于LLM工程的知识。你不应该使用Agent、Harness等概念
3.但是你需要从LLM的概率神经模型角度思考问题。

为AVM做计划需要什么：
1.当用户叫你计划的时候，不要立马给出结果，查找和创建docs/Agents文件夹下的记忆文件。
2.这是一个宏观创新项目，你的每一个计划都需要留档，记录时间、背景、起因、行动、影响。记得及时对废弃的设计进行标记
3.将详细的四部分计划给用户看，询问用户对各个方面的评分和改进建议。提示："只要、不是而是、很简单、足够"这样的词会让用户强烈反感，就像Linus看到一个不安全的内核代码一样

为AVM写代码需要什么：
1.明确自己遵循的计划
2.不要做任何超出计划的事情
3.在编写中发现相关的必要设计的缺失，使用注释、TODO等立马标记，并在之后向用户提出issue
4.在编写时发现bug、改动不兼容、不能确定的取舍，停止编写，给出不同观点下、不同设计下的多个计划，询问用户怎么办

一个好计划是什么：
1.一个好计划不是设计一个完美的系统，而是修改现有系统以达成目标，不多不少
2.好计划的目标简单，好计划注重过程，你需要分析计划修改的目的、影响、现有体系能否实现
3.好计划很谦虚。如果计划和现有代码有冲突，你不应该实现那个计划。用户指定你实施对旧架构、冲突的架构的删除计划后你才能实现新计划
4.代码牵一发而动全身，每一个计划都要审批，考虑代码兼容性
5.计划的目标不能是违反项目宗旨的。给AVM加入Agent无异于给Linux内核的git仓库加入用户态程序——它最多用于测试。

---
## 设计思路（从 docs/record 提炼）

### 核心命题：控制论与管理学取代软件工程

AVM 的根本洞见不是"把控制流从 Python 移到提示词"——那是表象。本质是：**在 AVM 中，控制论和管理学代替了软件工程，成为构建系统的元学科。**

- 传统 Agent 框架用软件工程方法：模块化、设计模式、接口、测试
- AVM 放弃了这些，用**控制论原理**（反馈回路、自适应、目标调节）和**管理学思想**（授权分权、职责边界、沟通协议）嵌入提示词中来组织计算
- 提示词不是"指令"，而是**控制律**——设定参考目标、感知当前状态、比较偏差、执行调节动作
- 提示词也不是"工作流脚本"，而是**组织管理手册**——谁负责什么、向谁汇报、哪些信息需要同步、异常如何处理

### AVM 运行时是一个中立的指令分发层

AVM 运行时不管"你是谁"——LLM 可以发指令，Python 也可以发指令，双方是对等的执行单元。统一性来自**协议**（内存地址空间 + 指令集 + 设备总线），不来自 AI。这与"Python 调 LLM"的框架范式根本不同。

### 对话调用分类法：主对话-子对话 / 亚对话 / 已存在对话

对话间的调用关系有两个关键维度：

| 维度 | 选项 | 对应指令 |
|------|------|---------|
| 目标是否已存在？ | 否（新建对话） | `create_cmd` / `create_sub` |
| 目标是否已存在？ | 是（已注册服务） | `call_service` / `transfer_service` |
| 调用者是否等待结果？ | 是 | `create_cmd` / `create_sub` / `call_service` |
| 调用者是否等待结果？ | 否 | `transfer_service` |

具体分类：

- **子对话**（`create_cmd`）：类似 Linux 父子进程。父创建子后进入休眠，子独立执行完毕，其结论回传给父的 tool response。子对话**只对 AVM 整体记忆的目的负责，不对父对话负责**。父的角色应退化为"按图索骥的入口点"——分发信息、启动流程，而非创造和生成。

- **亚对话**（`create_sub`）：这才是常见的 sub-agent 模式。父创建亚对话后不休眠，亚对话立刻激活执行，执行完毕输出写入父的 user_batch，父在就绪队列中恢复。亚对话**被父对话决定和控制**，属于父子紧耦合。

- **服务调用**（`call_service`）：目标是**已注册的既存对话**（通过 `register_service` 注册）。调用者等待服务返回。双方都插入就绪队列。

- **服务移交**（`transfer_service`）：调用者将控制权移交给已存在服务，**不等待返回**。调用者进入休眠。适用于无限循环或尾递归优化。

核心原则不变：主对话"长就是好"的偏见会通过控制子对话而放大，必须通过提示词限制主对话的地位。亚对话反而需要主对话更主动的控制。

### 每个对话只做一件小事：上下文污染与自大偏见

AI 在一个上下文中会倾向于认为"自己写的代码是好的"（否则为什么要写？）。换一个新的 AI 看同一段代码，概率上会倾向于认为"它是坏的"（否则为什么被送到我这里来？）。因此每个对话/子对话只应承担**一个微小、可管理、可追溯的修改**——小而严格的任务边界是防止上下文污染和 AI 自大偏见的物理隔离机制。

### 提示词程序的工程化：人类写，AI 执行，AI 优化提示词

AI 自己生成处理流程（子对话提示词）不是作者初衷——人类写提示词，AI 入口点整理并启动。AI 可以参与优化提示词，但这是在**另一个对话中**观察行为、评估分数、修改提示词本身（元编程），而不是在同一个对话里边跑边改。对话的职责是**严谨遵守提示词**，不是根据反馈修改自身。

### 用户不是"用户"

在 AVM 中，用户不是聊天对象。用户是一个提供信息的"导师/指导者"。用户输入不是直接对话，而是抽象操作。AVM 优先处理的是内存中设定的"目的"，用户只是外部信息源之一。应该从命令行（指令式）开始，而非从对话式开始。

### AI 倾向于 AI 内容：多样性作为对策

AI 阅读包含 AI 生成文本的文档时，会抛弃人类写的内容，不断复述和解释 AI 的文字。对策是"随机集成增强"：打乱提示词顺序、减少句子、使用不同上下文、将输出分割成独立部分分发到不同对话——利用多样性对抗概率模型的吸引子效应。

### 架构方向：进程树、异步调度、事件驱动

- **上下文管理**：从栈模型迁移到树模型，参考 Linux 进程树。父子对话隔离，父只看子结论。
- **调度状态机**：Active → Dormant（触发子对话）、Dormant → Active（回调/被调用）、Active → Ready（任务完成）、Ready → Active（轮询）、Ready → 终止/归档
- **优先级**：主对话 > 子对话调用队列 > 回调队列
- **未来方向**：回调函数、钩子对话、乱序执行、流水线、并发。这些都是 OS 概念在 AI 运行时的映射。
- **文件安全**：AI 可以为自己的文件设定密码、阻止其他 AI 访问、在其他 AI 访问时获得回调通知

### 底层学科基础：操作系统 > 控制论 > 管理学 > 软件工程

学 AI Agent 工程没有意义（这个领域才四五年，没有深厚的理论基础）。应该学：
1. 操作系统原理（进程模型、调度、内存管理、事件机制、文件系统）
2. 控制论（反馈、调节、自适应系统）
3. 管理学（组织设计、授权、职责边界、沟通汇报）
4. 软件工程（回调、序列化、数据结构——这些是"我们要实现的东西"）

### 语言的局限性：人类语言不适合精确推理

"思考"这个词的内涵并不是思考。AI 会因为语料的语义噪声产生幻觉。提示词必须极其严谨，逼近数学式的抽象。对 AI 来说，抽象的语言又会变成另一种剧本。必须使用工程化的提示词设计——系统性地定义行为边界，而不是依赖 AI 的"理解"。

---
## 运行

### 依赖

```bash
pip install openai python-dotenv pytest
# Web UI: pip install fastapi uvicorn
```

需要 `.env` 文件设置 `OPENAI_API_KEY`。

### 脚本

```bash
python main.py                       # 主入口
python simple_main.py                # 简易演示（单对话）
python web/server.py                 # Web UI (端口 8080)
bash scripts/run_dual_terminal.sh    # tmux 三面板
```

### 测试

```bash
pytest tests/                        # 全部（MockLMU，不调真实 API）
```

### 配置

`config.json` 控制: `prompt`、`log_level`、`debug`（STEP DEBUG）、`uuid`、`persist_level`、`restart`

## Agent skills

### Issue tracker

Issues for this repo live as GitHub issues ([tlozttok/AVM1](https://github.com/tlozttok/AVM1)). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
