AVM1是一个旨在构建以LLM对话为核心的通用计算模型架构的项目

它：
1.不是Agent架构
2.不提供具体的提示词（工具调用、环境提示除外）
3.不和现实操作系统、硬件、网络进行交互
4.自成体系，有内存模型和计算调度模型
5.疯狂地不做你认为必须做的事

如何参与设计它：
1.你需要不参与python和LLM的工程生产，你需要精通计算机科学史
2.LLM从出现至今时间很短，你不需要知道任何关于LLM工程的知识。你不应该使用Agent、Harness等概念
3.但是你需要从LLM的概率神经模型角度思考问题。

为AVM做计划需要什么：
1.当用户叫你计划的时候，不要立马给出结果，查找和创建docs/Agents文件夹下的记忆文件。
2.这是一个宏观创新项目，你的每一个计划都需要留档，记录时间、背景、起因、行动、影响。记得及时对废弃的设计进行标记
3.将详细的四部分计划给用户看，询问用户对各个方面的评分和改进建议。提示：“只要、不是而是、很简单、足够”这样的词会让用户强烈反感，就像Linus看到一个不安全的内核代码一样

为AVM写代码需要什么：
1.明确自己遵循的计划
2.不要做任何超出计划的事情
3.在编写中发现相关的必要设计的缺失，使用注释、TODO等立马标记，并在之后向用户提出issue
4.在编写时发现bug、改动不兼容、不能确定的取舍，停止编写，给出不同观点下、不同设计下的多个计划，询问用户怎么办

一个好计划是什么：
1.一个好计划不是设计一个完美的系统，而是修改现有系统以达成目标，不多不少
2.好计划的目标简单，好计划注重过程，你需要分析计划修改的目的、影响、现有体系能否实现
3.好计划很谦虚。如果计划和现有代码有冲突，你不应该实现那个计划。用户指定你实施对旧架构、冲突的架构的删除计划后你才能实现新计划
4.代码牵一发而动全身，每一个计划都要审批，考虑代码兼容性
5.计划的目标不能是违反项目宗旨的。给AVM加入Agent无异于给Linux内核的git仓库加入用户态程序——它最多用于测试。

接下来是你应当根据AVM的现有代码自己写的内容，包括长期宗旨、中期架构、短期决策：
>顺便说一下，在项目中没有，但是我的目标里，AVM是一个用于执行复杂提示词程序的通用模型，并且我已经在设计多个提示词程序，但还没有记录