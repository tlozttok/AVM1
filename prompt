1. MEM 类化
将全局 MEM 改为 Memory 类，内部用字典存储。

实现 __getitem__、__setitem__、__contains__ 等魔术方法。

将 Core._mem_unwrap 的逻辑移入 Memory.unwrap 方法，支持 $（递归解引用）和 &（一层解引用），以及 MetaList/MetaDict 的 to_llm_string() 转换。

Core.unwrap 只处理 $last_msg_reg 和 $usr_tool_reg 的寄存器访问，其他情况调用 self.mem.unwrap。

2. 删除 return_key_ref，加入 utr_index 和 call_id
CreateInstruction：

参数改为 (system_ref, user_ref, para_ref, mode, utr_index, call_id)

执行时调用 lmu.exec_crt（传入当前 utr_index，以便 create_cmd 工具使用）

如果有 result（非空）：

若 mode == 'a'：向 usr_tool_reg[utr_index] 追加 (result, call_id)（作为工具响应）

若 mode == 'w'：替换 usr_tool_reg[utr_index] 为新的 UserMessageBatch 并添加该响应

如果有 return_calls：

先分配新的寄存器槽位：last_msg_idx = len(last_msg_reg)，user_msg_idx = len(usr_tool_reg)

存入新创建的 Conversation（来自 lmu.exec_crt 的 result 和 return_calls 构建的？实际上 exec_crt 返回的 result 是文本，return_calls 是指令列表，需要根据这些构建一个新的 Conversation 对象？原代码中 exec_crt 没有返回 conversation，只返回 result 和 return_calls。但 create 指令的后续 exec 需要 Conversation。所以 LMU.exec_crt 也需要返回一个 Conversation 对象，包含 system 和 user 消息，以及 assistant 的回复（如果有）。

将栈顶替换为 exec $last_msg_reg.{last_msg_idx} $user_tool_reg.{user_msg_idx} {para_ref} {mode} {utr_index} {call_id}

压入 return_calls（每个元素是完整的指令字符串）

如果没有 return_calls，则直接弹出当前指令。

ExecInstruction：

参数改为 (last_msg_ref, user_msg_ref, para_ref, mode, utr_index, call_id_ref)

执行时：

从 last_msg_ref 解引用得到 Conversation（这个对话是临时的，用完即弃）

从 user_msg_ref 解引用得到 UserMessageBatch（包含当前要发送的工具响应）

调用 lmu.exec(conversation, user_batch, para, current_utr_index=utr_index)，返回 (result, return_calls, updated_conversation)（但 updated_conversation 不需要保存）

如果有 result：

弹出指令栈

解引用 call_id_ref 得到 call_id 字符串

将 (result, call_id) 存入 usr_tool_reg[utr_index]（追加或覆盖，根据 mode）

注意：user_msg_ref 和 last_msg_ref 对应的寄存器槽位是否需要清理？原代码会 pop 掉，但现在对话不需要保留，可以 pop 掉 last_msg_reg 和 usr_tool_reg 中对应的条目，以释放资源。但注意 utr_index 可能和这些索引不同，不能混淆。

如果有 return_calls：

不弹出当前指令，直接压入 return_calls（这样当前 exec 会再次执行，使用新的 user_msg_ref 中的响应）

3. 工具调用类型化，添加 create_cmd 工具
LMU 修改：

exec_crt 和 exec 方法增加参数 current_utr_index: int。

定义两个工具：command 和 create_cmd。

command：参数 {"command": str}，解析后直接取 command 字段值作为指令字符串加入 return_calls。

create_cmd：参数 {"system_ref": str, "user_ref": str, "para_ref": str, "mode": str, "call_id": str}，构造指令字符串：

text
f"create {system_ref} {user_ref} {para_ref} {mode} {current_utr_index} {call_id}"
加入 return_calls。

工具调用设计：

command 工具：参数包含 command（指令字符串）和 call_id（字符串）。
LMU 解析后，生成指令字符串时，需要在原 command 字符串末尾追加 call_id（因为所有指令的最后一个参数都是 call_id）。例如，LLM 返回 {"command": "create $a $b $c w 5", "call_id": "call_123"}，则生成的指令为 "create $a $b $c w 5 call_123"。

create_cmd 工具：参数包含 system_ref, user_ref, para_ref, mode, call_id（不需要 utr_index，由 VM 注入）。
LMU 生成指令：f"create {system_ref} {user_ref} {para_ref} {mode} {current_utr_index} {call_id}"（注意 utr_index 是当前指令的父寄存器索引，由 VM 自动填入）。

其他指令（如 set_metadata）：如果需要被工具调用，也可以通过 command 工具生成，并同样附加 call_id。指令解析器 parse_instruction 需要能解析出最后的 call_id 参数（对于 set_metadata 这种参数数量不固定的指令，约定 call_id 为最后一个参数，可以是任何字符串）。

指令执行逻辑（所有指令统一）：

执行后若有 result（非 None），则根据 mode 将 (result, self.call_id) 存入 core.usr_tool_reg[self.utr_index]。

若有 return_calls，则压栈子指令（子指令的 utr_index 为当前指令的 utr_index，因为结果要返回到同一个父寄存器）。